# タスクリスト

## フェーズ1: ボトムサマリーの簡素化
- [x] `bottom_summary_widget.dart` の `_buildSharedModeTotalDisplay` を廃止
- [x] 共有モード時も非共有モード時と同じ「合計金額」表示に統一
- [x] `_calculateCurrentShopTotal()` の計算結果を「合計金額」として表示
- [x] 不要になった共有合計関連のキャッシュ変数を整理

## フェーズ2: タブバーに共有合計を表示
- [x] `main_app_bar.dart` の `_buildTabItem` で共有グループの最後のタブに金額を追加
- [x] 同期的に計算（`sortedShops` から直接計算、非同期不要に）
- [x] 金額表示のスタイリング（バッジ風の小さなラベル）
- [x] タブの高さ調整不要（Row内に収まる設計）

## フェーズ3: テスト・動作確認
- [ ] 共有モードのボトムサマリー表示確認
- [ ] 非共有モードのボトムサマリー表示確認（回帰なし）
- [ ] タブバーの共有合計表示確認
- [ ] 予算機能・進捗バーへの影響がないことを確認
- [ ] flutter analyze でLintエラーなし

## 依存関係
- フェーズ1 → フェーズ2（並行可能だが、ボトムサマリー側を先に整理すると分かりやすい）
- フェーズ1, 2 → フェーズ3（実装完了後にテスト）
