# テスト計画: Issue #47 ディレクトリ構成の統一

## テスト方針

純粋なリファクタリング（ファイル移動 + インポートパス変更）のため、機能テストは不要。
ビルドの正常性確認のみ。

## 自動テスト

### T1: 静的解析
```bash
flutter analyze
```
- 全てのインポートパスが正しく解決されること
- Lintエラーがないこと

### T2: 既存テスト
```bash
flutter test
```
- 全テストが通過すること（既存テストにdrawer/ad参照なし確認済み）

## 手動確認

### T3: ビルド確認（オプション）
- `flutter build web` が成功すること
- アプリ起動時にエラーが発生しないこと

## 回帰テスト

- 既存の機能に変更はないため、テスト通過をもって回帰テスト完了とする
